@page "/fieldvalidators"

<PageTitle>FieldValidator - BlazyUI</PageTitle>

<h1 class="text-3xl font-bold mb-8">FieldValidator</h1>

<p class="mb-8 text-base-content/70">
    The FieldValidator component combines DaisyUI's HTML5 CSS-based validation hints with Blazor's ValidationMessage.
    It cascades a context to child inputs, automatically enabling the <code>validator</code> CSS class for immediate visual feedback.
</p>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">Basic HTML5 Validation</h2>
    <p class="mb-4 text-sm text-base-content/70">Uses HTML5 validation attributes (required, type="email") with immediate CSS-based feedback.</p>
    <CodeExample Code="@basicCode">
        <form class="max-w-xs flex flex-col gap-4">
            <div>
                <BlazyFieldValidator TValue="string" Hint="Please enter your email">
                    <BlazyTextInput @bind-Value="basicEmail" Type="email" placeholder="Email" required />
                </BlazyFieldValidator>
            </div>
            <BlazyButton Type="submit" Color="BlazyButtonColor.Primary">Submit</BlazyButton>
        </form>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">With Blazor EditForm</h2>
    <p class="mb-4 text-sm text-base-content/70">Combines HTML5 validation with Blazor's DataAnnotationsValidator for server-side validation.</p>
    <CodeExample Code="@editFormCode">
        <EditForm Model="@formModel" OnValidSubmit="@HandleValidSubmit" class="max-w-xs">
            <DataAnnotationsValidator />

            <div class="flex flex-col gap-4">
                <div>
                    <BlazyFieldValidator For="@(() => formModel.Email)" Hint="Invalid email format">
                        <BlazyTextInput @bind-Value="formModel.Email" Type="email" placeholder="Email" required />
                    </BlazyFieldValidator>
                </div>

                <div>
                    <BlazyFieldValidator For="@(() => formModel.Password)" Hint="Password is required">
                        <BlazyTextInput @bind-Value="formModel.Password" Type="password" placeholder="Password" required minlength="6" />
                    </BlazyFieldValidator>
                </div>

                <BlazyButton Type="submit" Color="BlazyButtonColor.Primary" Block>Sign In</BlazyButton>
            </div>
        </EditForm>
        @if (formSubmitted)
        {
            <BlazyAlert Color="BlazyAlertColor.Success" class="mt-4">Form submitted successfully!</BlazyAlert>
        }
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">HTML5 Only Mode</h2>
    <p class="mb-4 text-sm text-base-content/70">Shows only HTML5 validation hints without Blazor ValidationMessage.</p>
    <CodeExample Code="@html5OnlyCode">
        <form class="max-w-xs flex flex-col gap-4">
            <div>
                <BlazyFieldValidator TValue="string" Mode="BlazyFieldValidatorMode.Html5Only" Hint="Required field">
                    <BlazyTextInput @bind-Value="html5Value" placeholder="Name" required />
                </BlazyFieldValidator>
            </div>
            <div>
                <BlazyFieldValidator TValue="string" Mode="BlazyFieldValidatorMode.Html5Only" Hint="Must be a valid URL">
                    <BlazyTextInput @bind-Value="urlValue" Type="url" placeholder="Website URL" />
                </BlazyFieldValidator>
            </div>
            <BlazyButton Type="submit" Color="BlazyButtonColor.Primary">Submit</BlazyButton>
        </form>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">Blazor Only Mode</h2>
    <p class="mb-4 text-sm text-base-content/70">Shows only Blazor ValidationMessage without HTML5 hints.</p>
    <CodeExample Code="@blazorOnlyCode">
        <EditForm Model="@blazorOnlyModel" OnValidSubmit="@HandleBlazorOnlySubmit" class="max-w-xs">
            <DataAnnotationsValidator />

            <div class="flex flex-col gap-4">
                <div>
                    <BlazyFieldValidator For="@(() => blazorOnlyModel.Username)" Mode="BlazyFieldValidatorMode.BlazorOnly">
                        <BlazyTextInput @bind-Value="blazorOnlyModel.Username" placeholder="Username" />
                    </BlazyFieldValidator>
                </div>
                <BlazyButton Type="submit" Color="BlazyButtonColor.Primary" Block>Submit</BlazyButton>
            </div>
        </EditForm>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">Rich Hint Content</h2>
    <p class="mb-4 text-sm text-base-content/70">Use HintContent for complex validation hints with formatting.</p>
    <CodeExample Code="@richHintCode">
        <form class="max-w-xs">
            <div>
                <BlazyFieldValidator TValue="string">
                    <ChildContent>
                        <BlazyTextInput @bind-Value="passwordValue" Type="password" placeholder="Password" required minlength="8" />
                    </ChildContent>
                    <HintContent>
                        <ul class="text-xs list-disc ml-4">
                            <li>At least 8 characters</li>
                            <li>One uppercase letter</li>
                            <li>One number</li>
                        </ul>
                    </HintContent>
                </BlazyFieldValidator>
            </div>
        </form>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">With Select</h2>
    <p class="mb-4 text-sm text-base-content/70">FieldValidator works with Select and other input components.</p>
    <CodeExample Code="@selectCode">
        <form class="max-w-xs flex flex-col gap-4">
            <div>
                <BlazyFieldValidator TValue="string" Hint="Please select a country">
                    <BlazySelect @bind-Value="selectedCountry" required>
                        <option value="">Choose a country...</option>
                        <option value="US">United States</option>
                        <option value="UK">United Kingdom</option>
                        <option value="CA">Canada</option>
                    </BlazySelect>
                </BlazyFieldValidator>
            </div>
            <BlazyButton Type="submit" Color="BlazyButtonColor.Primary">Submit</BlazyButton>
        </form>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">With Textarea</h2>
    <p class="mb-4 text-sm text-base-content/70">FieldValidator also works with Textarea components.</p>
    <CodeExample Code="@textareaCode">
        <form class="max-w-md flex flex-col gap-4">
            <div>
                <BlazyFieldValidator TValue="string" Hint="Please enter at least 10 characters">
                    <BlazyTextarea @bind-Value="bioValue" placeholder="Tell us about yourself..." required minlength="10" rows="3" />
                </BlazyFieldValidator>
            </div>
            <BlazyButton Type="submit" Color="BlazyButtonColor.Primary">Submit</BlazyButton>
        </form>
    </CodeExample>
</section>

<section class="mb-12">
    <h2 class="text-xl font-semibold mb-4">Always Show Hint</h2>
    <p class="mb-4 text-sm text-base-content/70">Set HideHintWhenValid="false" to always show the hint regardless of validation state.</p>
    <CodeExample Code="@alwaysShowCode">
        <form class="max-w-xs">
            <div>
                <BlazyFieldValidator TValue="string" Hint="Enter a valid email address" HideHintWhenValid="false">
                    <BlazyTextInput @bind-Value="alwaysShowEmail" Type="email" placeholder="Email" />
                </BlazyFieldValidator>
            </div>
        </form>
    </CodeExample>
</section>

@code {
    private string basicEmail = "";
    private string html5Value = "";
    private string urlValue = "";
    private string passwordValue = "";
    private string selectedCountry = "";
    private string bioValue = "";
    private string alwaysShowEmail = "";
    private bool formSubmitted = false;

    private FormModel formModel = new();
    private BlazorOnlyModel blazorOnlyModel = new();

    private void HandleValidSubmit()
    {
        formSubmitted = true;
    }

    private void HandleBlazorOnlySubmit()
    {
        // Handle submission
    }

    private class FormModel
    {
        [System.ComponentModel.DataAnnotations.Required(ErrorMessage = "Email is required")]
        [System.ComponentModel.DataAnnotations.EmailAddress(ErrorMessage = "Invalid email format")]
        public string Email { get; set; } = "";

        [System.ComponentModel.DataAnnotations.Required(ErrorMessage = "Password is required")]
        [System.ComponentModel.DataAnnotations.MinLength(6, ErrorMessage = "Password must be at least 6 characters")]
        public string Password { get; set; } = "";
    }

    private class BlazorOnlyModel
    {
        [System.ComponentModel.DataAnnotations.Required(ErrorMessage = "Username is required")]
        [System.ComponentModel.DataAnnotations.StringLength(20, MinimumLength = 3, ErrorMessage = "Username must be 3-20 characters")]
        public string Username { get; set; } = "";
    }

    private string basicCode = @"<form>
    <BlazyFieldValidator TValue=""string"" Hint=""Please enter your email"">
        <BlazyTextInput @bind-Value=""email"" Type=""email"" placeholder=""Email"" required />
    </BlazyFieldValidator>
    <BlazyButton Type=""submit"">Submit</BlazyButton>
</form>";

    private string editFormCode = @"<EditForm Model=""@formModel"" OnValidSubmit=""@HandleValidSubmit"">
    <DataAnnotationsValidator />

    <BlazyFieldValidator For=""@(() => formModel.Email)"" Hint=""Invalid email format"">
        <BlazyTextInput @bind-Value=""formModel.Email"" Type=""email"" placeholder=""Email"" required />
    </BlazyFieldValidator>

    <BlazyFieldValidator For=""@(() => formModel.Password)"" Hint=""Password is required"">
        <BlazyTextInput @bind-Value=""formModel.Password"" Type=""password"" placeholder=""Password"" required minlength=""6"" />
    </BlazyFieldValidator>

    <BlazyButton Type=""submit"" Color=""BlazyButtonColor.Primary"">Sign In</BlazyButton>
</EditForm>";

    private string html5OnlyCode = @"<form>
    <BlazyFieldValidator TValue=""string"" Mode=""BlazyFieldValidatorMode.Html5Only"" Hint=""Required field"">
        <BlazyTextInput @bind-Value=""name"" placeholder=""Name"" required />
    </BlazyFieldValidator>
    <BlazyFieldValidator TValue=""string"" Mode=""BlazyFieldValidatorMode.Html5Only"" Hint=""Must be a valid URL"">
        <BlazyTextInput @bind-Value=""url"" Type=""url"" placeholder=""Website URL"" />
    </BlazyFieldValidator>
</form>";

    private string blazorOnlyCode = @"<EditForm Model=""@model"" OnValidSubmit=""@HandleSubmit"">
    <DataAnnotationsValidator />

    <BlazyFieldValidator For=""@(() => model.Username)"" Mode=""BlazyFieldValidatorMode.BlazorOnly"">
        <BlazyTextInput @bind-Value=""model.Username"" placeholder=""Username"" />
    </BlazyFieldValidator>

    <BlazyButton Type=""submit"">Submit</BlazyButton>
</EditForm>";

    private string richHintCode = @"<BlazyFieldValidator TValue=""string"">
    <ChildContent>
        <BlazyTextInput @bind-Value=""password"" Type=""password"" placeholder=""Password"" required minlength=""8"" />
    </ChildContent>
    <HintContent>
        <ul class=""text-xs list-disc ml-4"">
            <li>At least 8 characters</li>
            <li>One uppercase letter</li>
            <li>One number</li>
        </ul>
    </HintContent>
</BlazyFieldValidator>";

    private string selectCode = @"<form>
    <BlazyFieldValidator TValue=""string"" Hint=""Please select a country"">
        <BlazySelect @bind-Value=""country"" required>
            <option value="""">Choose a country...</option>
            <option value=""US"">United States</option>
            <option value=""UK"">United Kingdom</option>
        </BlazySelect>
    </BlazyFieldValidator>
</form>";

    private string textareaCode = @"<form>
    <BlazyFieldValidator TValue=""string"" Hint=""Please enter at least 10 characters"">
        <BlazyTextarea @bind-Value=""bio"" placeholder=""Tell us about yourself..."" required minlength=""10"" />
    </BlazyFieldValidator>
</form>";

    private string alwaysShowCode = @"<BlazyFieldValidator TValue=""string"" Hint=""Enter a valid email address"" HideHintWhenValid=""false"">
    <BlazyTextInput @bind-Value=""email"" Type=""email"" placeholder=""Email"" />
</BlazyFieldValidator>";
}
