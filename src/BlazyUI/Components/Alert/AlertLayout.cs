namespace BlazyUI;

/// <summary>
/// Layout direction for the Alert component.
/// </summary>
public enum AlertLayout
{
    /// <summary>Default layout.</summary>
    Default,
    /// <summary>Vertical layout (stacked content).</summary>
    Vertical,
    /// <summary>Horizontal layout (inline content).</summary>
    Horizontal
}
