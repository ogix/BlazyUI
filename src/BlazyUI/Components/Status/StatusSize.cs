namespace BlazyUI;

/// <summary>
/// Size variants for the Status component.
/// </summary>
public enum StatusSize
{
    /// <summary>Extra small size (status-xs).</summary>
    ExtraSmall,
    /// <summary>Small size (status-sm).</summary>
    Small,
    /// <summary>Medium size (status-md). Default.</summary>
    Medium,
    /// <summary>Large size (status-lg).</summary>
    Large,
    /// <summary>Extra large size (status-xl).</summary>
    ExtraLarge
}
