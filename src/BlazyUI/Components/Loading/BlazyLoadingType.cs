namespace BlazyUI;

/// <summary>
/// Animation type variants for the Loading component.
/// </summary>
public enum BlazyLoadingType
{
    /// <summary>Rotating spinner animation (loading-spinner).</summary>
    Spinner,
    /// <summary>Animated dots effect (loading-dots).</summary>
    Dots,
    /// <summary>Ring animation (loading-ring).</summary>
    Ring,
    /// <summary>Ball animation (loading-ball).</summary>
    Ball,
    /// <summary>Bars animation (loading-bars).</summary>
    Bars,
    /// <summary>Infinity symbol animation (loading-infinity).</summary>
    Infinity
}
