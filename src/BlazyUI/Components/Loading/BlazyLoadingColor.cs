namespace BlazyUI;

/// <summary>
/// Color variants for the Loading component.
/// </summary>
public enum BlazyLoadingColor
{
    /// <summary>No color class applied, inherits parent text color.</summary>
    None,
    /// <summary>Primary color (text-primary).</summary>
    Primary,
    /// <summary>Secondary color (text-secondary).</summary>
    Secondary,
    /// <summary>Accent color (text-accent).</summary>
    Accent,
    /// <summary>Neutral color (text-neutral).</summary>
    Neutral,
    /// <summary>Info color (text-info).</summary>
    Info,
    /// <summary>Success color (text-success).</summary>
    Success,
    /// <summary>Warning color (text-warning).</summary>
    Warning,
    /// <summary>Error color (text-error).</summary>
    Error
}
