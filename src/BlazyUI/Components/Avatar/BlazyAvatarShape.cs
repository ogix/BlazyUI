namespace BlazyUI;

/// <summary>
/// Shape variants for the Avatar component.
/// </summary>
public enum BlazyAvatarShape
{
    /// <summary>Circular avatar (rounded-full). Default.</summary>
    Circle,
    /// <summary>Rounded square avatar (rounded-xl).</summary>
    Rounded,
    /// <summary>Squircle shape avatar (mask mask-squircle).</summary>
    Squircle
}
