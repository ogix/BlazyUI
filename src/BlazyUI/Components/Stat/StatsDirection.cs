namespace BlazyUI;

/// <summary>
/// Direction/layout variants for the Stats container component.
/// </summary>
public enum StatsDirection
{
    /// <summary>No direction modifier (responsive default).</summary>
    Default,
    /// <summary>Horizontal layout (stats-horizontal).</summary>
    Horizontal,
    /// <summary>Vertical layout (stats-vertical).</summary>
    Vertical
}
