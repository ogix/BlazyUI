namespace BlazyUI;

/// <summary>
/// Text position for the Divider component.
/// </summary>
public enum BlazyDividerPosition
{
    /// <summary>Text centered (default, no class).</summary>
    Default,
    /// <summary>Text aligned to the start.</summary>
    Start,
    /// <summary>Text aligned to the end.</summary>
    End
}
