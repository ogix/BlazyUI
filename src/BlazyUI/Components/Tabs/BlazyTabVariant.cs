namespace BlazyUI;

/// <summary>
/// Style variants for the Tabs component.
/// </summary>
public enum BlazyTabVariant
{
    /// <summary>Default tab style.</summary>
    Default,
    /// <summary>Tabs with border separator (tabs-border).</summary>
    Border,
    /// <summary>Lifted tab style (tabs-lift).</summary>
    Lift,
    /// <summary>Boxed tab style (tabs-box).</summary>
    Box
}
